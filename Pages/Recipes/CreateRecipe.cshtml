@page
@model CreateRecipeModel
@using HandleLet;
@using HandleLet.Interfaces;
@inject IRecipe repo
@{
}

<h1>Opret ny vare</h1>

<form method="post">
    @if (Model.errorMessage.Length > 0)
    {
        <div>
            <label class="text-danger">ID findes allerede</label>
        </div>
    }

    <div class="form-group">
        <label asp-for="@Model.recipe.id" class="control-label">ID</label>
        <input asp-for="@Model.recipe.id" class="form-control" />
        <span asp-validation-for="@Model.recipe.id" class="text-danger"></span>
    </div>
    <p></p>
    <p></p>
    <div>
        <label asp-for="@Model.recipe.Title" class="control-label">Titel</label>
        <input asp-for="@Model.recipe.Title" class="form-control" />
        <span asp-validation-for="@Model.recipe.Title" class="text-danger"></span>
    </div>
    <p></p>
    <div class="form-group">
        <label asp-for="@Model.recipe.Description" class="control-label">Beskrivelse</label>
        <input asp-for="@Model.recipe.Description" class="form-control" />
        <span asp-validation-for="@Model.recipe.Description" class="text-danger"></span>
    </div>
    <p></p>

        <p></p>
        <div class="form-group">
            <input type="submit" value="Opret" class="btn btn-primary" />
            <input type="reset" value="Nulstil" class="btn brn-primary" />
        </div>
        <a asp-page="/Recipes/Recipe">Tilbage til alle varer</a>
   
</form>
